query NODE_LIST_QUERY($first: Int!, $after: String) {
  taxRates(first: $first, after: $after) {
    nodes {
      city
      class
      compound
      country
      databaseId
      id
      name
      order
      postcode
      priority
      rate
      shipping
      state
      __typename
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
